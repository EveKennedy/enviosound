# EnvioSound

A polished mobile iOS-style prototype for travel vlog soundtrack discovery and generation.

## Run

```bash
npm install
npm run dev
```

The app uses static mock data only. No backend, login, payments, or real audio generation are included.
